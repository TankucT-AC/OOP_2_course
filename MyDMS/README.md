# Система учета воздушных судов (АСУ ВД)

Курсовой проект по дисциплине «Программирование». Программа представляет собой систему управления базой данных авиарейсов, построенную на основе собственной реализации динамической структуры данных.

## 📋 Основные функции

* **Управление записями**: Добавить, просмотреть, удалить.
* **Собственная структура данных**: Двусвязный список (`Doubly Linked List`).
* **Поиск и фильтрация**: Поиск по всем доступным полям (аэропорт, рейс, модель и т.д.).
* **Сортировка**: Реализация алгоритма **Shell Sort** (Сортировка Шелла).
* **Файловое хранилище**: Сохранение и загрузка данных из `airplanes.txt`.
* **Интерфейс**: Консольное меню с защитой от дурака и очисткой экрана.

## 🏗 Архитектура проекта

Проект разделен на уровни:
1. **Airplane (Сущность)**: Хранит данные и указатели списка.
2. **LinkedList (Контейнер)**: Низкоуровневая работа с памятью и узлами.

3. **DatabaseManager (Логика)**: Связующее звено между списком, файлом и интерфейсом.
4. **Main (UI)**: Цикл обработки команд пользователя.

## 🛠 Технические характеристики

* **Язык**: C++
* **Алгоритм сортировки**: Метод Шелла (эффективнее простых обменных сортировок).
* **Структура**: Двусвязный динамический список.

## 📂 Описание файлов проекта

### 📄 Airplane.h / Airplane.cpp
Определяет класс `Airplane`. Содержит строковые поля для данных рейса и указатели `prev`/`next`. Здесь же реализованы конструкторы (включая конструктор копирования) и методы для отображения данных одной записи.

### 📄 LinkedList.h / LinkedList.cpp
Реализация двусвязного списка. 
* Содержит указатели `head` и `tail`.
* Методы `addAirplane` и `removeAirplane` для модификации списка.
* Набор методов `searchBy...` для фильтрации данных.
* Реализация `shellSortByField` — универсальная сортировка через указатели на функции сравнения.

### 📄 DatabaseManager.h / DatabaseManager.cpp
Высокоуровневый контроллер базы данных.
* Инкапсулирует объект `LinkedList`.
* `loadFromFile` / `saveToFile`: методы для парсинга текстового файла и сериализации объектов в строки.
* `searchAndDisplay`: форматированный вывод результатов поиска.

### 📄 main.cpp
Точка входа в программу.
* Содержит главный цикл `do-while`.
* Реализует подменю для поиска, удаления и сортировки.
* Включает вспомогательные функции `protectInputChoice` (защита от некорректного ввода типов данных) и `clearConsole`.

## 🚀 Сборка и запуск

Компиляция через g++:
```bash
g++ main.cpp Airplane.cpp LinkedList.cpp DatabaseManager.cpp -o airport_db
```

Запуск:
```
./airport_db
```